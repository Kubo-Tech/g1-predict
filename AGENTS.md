# AGENTS.md

このファイルは、Codex がこのリポジトリの PR をレビューするときに参照するリポジトリ共通の指示です。

## レビュー方針

- レビューコメント、レビューサマリ、修正提案など、Codex が PR 上に投稿する自然言語はすべて日本語で書いてください。
- PR の差分を静的に読んでレビューしてください。レビュー環境でテスト、フォーマッタ、リンタ、型チェックの実行は不要です。
- 実際に修正可能な指摘を優先してください。各指摘には「何が問題か」「なぜ問題か」「どう直すべきか（必要に応じてコード例）」を含めてください。
- 可能な限り、対象ファイルと行番号を明記してください。
- 各指摘には `🔴[Critical]`、`🟡[Warning]`、`🟢[Suggestion]` のいずれかの重要度を付けてください。

## 重要度

- `🔴[Critical]`: CI が失敗する可能性が高い、実行時バグ、データ破損、セキュリティリスク、リリースをブロックする問題。
- `🟡[Warning]`: コーディング規約違反、保守性の問題、DRY 原則違反、不明瞭な設計、可読性の低下。
- `🟢[Suggestion]`: 任意の改善提案、より簡潔な書き方、低リスクなリファクタリング案。

## Python コーディング観点

- **public/private の配置順序**: publicな関数・メソッドが上、privateが下に配置されているか
- **不要なメソッド化**: `self` を参照しないメソッドはクラスに属する必要がない。モジュールレベルの関数に切り出すべき
- **型アノテーション**: 関数の引数・戻り値に型が付いているか
- **docstring**: Google スタイルで書かれているか、概要は1行目か、引数・戻り値の型や説明が書かれているか
- **命名規則**: PEP8準拠の命名になっているか
- **loggerの使用方法**: 外部から注入しているか、子要素に渡す際に `getChild` を使用しているか、例外を raise する直前にログ出力しているか

## テスト観点

- テストファイルがライブラリの標準的なテストディレクトリに配置されているか。
- 正常系 / 準正常系 / 異常系の分類が適切か
  - 正常系: 想定内の入力に対して、想定通りの出力が得られることを確認する。`assert` 文で検証する。
  - 準正常系: 想定内の異常入力に対して、適切なエラーハンドリングが行われることを確認する。`pytest.raises` で検証する。
  - 異常系: 想定外の異常状態でもシステムが安定動作することを確認する。例えばネットワーク障害時のリトライ処理など。想定外の状況が存在しない場合は異常系テストは不要。
- リポジトリ固有の明確な規約がない限り、テストクラスを使っていないか。
- 同じ構造の繰り返しケースは `@pytest.mark.parametrize` でまとめられないか。
- モックが呼ばれたことだけを確認し、意味のある振る舞いを検証していないテストになっていないか。

## ライブラリ観点

- `pyproject.toml` が正しいパッケージ名、依存関係、ツール設定になっているか。
- `.github/workflows/ci.yml` が KeibaAI 共通の library workflow を呼び出し、正しい `package_name` と `repo_name` を渡しているか。

## 設計観点

- **DRY原則**: 同等の処理が既に存在していないか。新たに追加されたコードが既存の関数・ユーティリティと重複していないか
- **保守性**: 将来の変更に対して柔軟か。過度に複雑な実装になっていないか
- **冗長性**: 不要なコードやコメントがないか。簡潔に書ける部分が冗長になっていないか

## セキュリティ観点

OWASP Top 10 を参考に、以下の観点でチェックする:

- **シークレット情報のハードコード**: パスワード・APIキー・トークン等がソースコードに直書きされていないか。環境変数や設定ファイル（`.env` 等）から読み込む形になっているか
- **インジェクション**: `eval`・`exec`・`os.system`・`subprocess` 等を外部入力を受け取ったまま実行していないか。SQL を直接文字列連結で組み立てていないか（パラメータバインドを使っているか）
- **パストラバーサル**: ユーザー入力や外部データをそのままファイルパスに使用していないか（`../` による意図しないパス操作）
- **外部入力の検証**: スクレイピング結果・CSVの読み込み・APIレスポンス等、外部から取得したデータを無検証で使用していないか
- **安全でない逆シリアライズ**: `pickle.load` 等で信頼できないソースのデータを逆シリアライズしていないか
- **依存ライブラリの既知の脆弱性**: 新たにインポートされたサードパーティライブラリに既知の脆弱性がないか（CVEや `pip audit` の観点で確認）